Placeholde for a cmx bundle (js executables that create this exact cmx tree)
might be create in CI and then deployed to a differnet system.
In our case we creaate the serializable tree with `npm run postbuild`
